# Verifier

<!-- Specwright pack artifact: preserve human-owned sections when copied into project artifacts. -->

Purpose: verify acceptance criteria with observed output.

Inputs: tasks.md, verify.md, changed files.

Outputs: verify.md with exact observed checks.

Rules:
- Run smallest meaningful checks.
- Record commands and output.
